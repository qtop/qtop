Implemented and committed on branch `earnbot/sec-qtop-qtop-337`.

- Commit: `9c586a7`
- Files changed:
  - [docs/documentation.rst](/Users/zheliu/Documents/Codex/2026-05-11/now-go-out-and-make-at-2/data/work/qtop__qtop/docs/documentation.rst)

Why this addresses issue #337:
- Tightened the differential-debugging instructions to make the “first step” explicit and reproducible for cross-cluster bug hunting:
  - captures the exact run command and pinned version context per snapshot,
  - uses deterministic full-view file selection before diffing,
  - extracts the first meaningful diff hunk,
  - explicitly requires confirming both snapshots were from the same pinned build.
- This directly documents the requested workflow for working/failing run comparison without changing runtime behavior.

Verification run:
- `python3 -m compileall -q qtop_py && echo "compileall_ok"`
- `git status --short` (only pre-existing untracked `.omx/state/active-sessions/mcp-16373.json` was present).

Security impact (one line):
- No executable code paths changed; only documentation text was updated, so there is no security behavior impact.