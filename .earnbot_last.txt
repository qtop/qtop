Implemented and committed on branch `earnbot/sec-qtop-qtop-337`.

- Commit: `a176a95`
- Files changed:
  - [`.github/ISSUE_TEMPLATE/cluster-diff-debug.md`](./.github/ISSUE_TEMPLATE/cluster-diff-debug.md)

Why this addresses issue #337:
1. The issue asks for a reproducible two-cluster (or same-cluster) differential-debug workflow with explicit artifacts.
2. The template now explicitly collects:
   - separate commands for working and failing runs,
   - clusters used for each run,
   - a concrete command snippet to extract the first `qtop_fullview_*.out` diff hunk,
   - an explicit yes/no statement on whether the bug reproduces on the latest qtop version.

Verification run:
- `git diff --check`
- `git status --short` (only pre-existing untracked `.omx/state/active-sessions/mcp-19800.json` remains; no repo file changes)

Assumption:
- I could not execute actual HPC cluster runs in this environment, so I made a documentation/template-focused fix only.

Security impact:
- None (documentation-only, no runtime code/config behavior changed).