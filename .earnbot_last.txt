Implemented a minimal, issue-focused change on branch `earnbot/sec-qtop-qtop-337`.

- Changed files:
  - [`.github/ISSUE_TEMPLATE/cluster-diff-debug.md`](/Users/zheliu/Documents/Codex/2026-05-11/now-go-out-and-make-at-2/data/work/qtop__qtop/.github/ISSUE_TEMPLATE/cluster-diff-debug.md)

- What this fixes:
  - Adds a dedicated bug-report template for differential debugging that explicitly requires:
    - both working/failing screenshots,
    - reproducible paired runs (same pinned version),
    - required first-pass diff artifacts,
    - and an explicit latest-version reproducibility check.
  - This documents and enforces the exact reporting workflow requested in issue #337.

- Commit:
  - `dd37261` — `docs: add differential debugging issue template`

- Verification run:
  - `git add .github/ISSUE_TEMPLATE/cluster-diff-debug.md && git commit -m "docs: add differential debugging issue template"`

- Security impact (1-line):
  - No production/runtime behavior changes; only adds reporting/documentation content, so no direct security surface impact.

- Assumptions:
  - I treated this as a process/documentation issue (the issue asks for explicit differential-debugging artifacts, not an internal code defect).