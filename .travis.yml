# Note: Primary CI has moved to GitHub Actions (.github/workflows/).
# This Travis CI config is retained for reference only.
# See Makefile for local test/lint/coverage commands.

language: python

python:
  - "3.10"
  - "3.12"

install:
  - pip install pytest pytest-cov ruff

script:
  - make lint
  - make test
