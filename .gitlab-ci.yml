stages:
  - security
  - test
  - coverage
  - build
  - publish

include:
  - local: ci/security.gitlab-ci.yml
  - local: ci/common.gitlab-ci.yml
  - local: ci/test.gitlab-ci.yml
  - local: ci/coverage.gitlab-ci.yml
  - local: ci/build.gitlab-ci.yml

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  SECURE_LOG_LEVEL: info
  SAST_EXCLUDED_PATHS: "artifacts,build,dist,.venv,docs/images"
  SECRET_DETECTION_EXCLUDED_PATHS: "artifacts,build,dist,.venv,docs/images"
  DS_EXCLUDED_PATHS: "artifacts,build,dist,.venv,docs/images"
